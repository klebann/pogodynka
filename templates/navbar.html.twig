<nav class="navbar navbar-expand-sm navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('weather_name', {city: 'Szczecin'}) }}">Pogodynka</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link{% if (app.request.pathInfo == path('weather_name', {city: 'Szczecin'})) %} active{% endif %}"
                       aria-current="page"
                       href="{{ path('weather_name', {city: 'Szczecin'}) }}">Weather</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link{% if (app.request.pathInfo == path('app_location_index')) %} active{% endif %}"
                       href="{{ path('app_location_index') }}">Location</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if (app.request.pathInfo == path('app_measurement_index')) %} active{% endif %}"
                       href="{{ path('app_measurement_index') }}">Measurements</a>
                </li>
            </ul>
        </div>
        {% if is_granted('ROLE_USER') %}
            <a class="link-danger" href="{{ path('app_logout') }}">Logout</a>
        {% else %}
            <a class="link-light" href="{{ path('app_login') }}">Login</a>
        {% endif %}
    </div>
</nav>